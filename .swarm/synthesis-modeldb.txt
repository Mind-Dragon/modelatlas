Task hash 001-deerflow-modeldb. SYNTHESIS PHASE.

Wait for ALL domain files to exist:
- /home/work/hermesfixes/deerflow-modeldb/DOMAIN1-OPENAI.md
- /home/work/hermesfixes/deerflow-modeldb/DOMAIN2-ANTHROPIC.md
- /home/work/hermesfixes/deerflow-modeldb/DOMAIN3-GOOGLE.md
- /home/work/hermesfixes/deerflow-modeldb/DOMAIN4-XAI-DEEPSEEK.md
- /home/work/hermesfixes/deerflow-modeldb/DOMAIN5-CHINESE-PROVIDERS.md
- /home/work/hermesfixes/deerflow-modeldb/DOMAIN6-MISTRAL-AGGREGATORS.md
- /home/work/hermesfixes/deerflow-modeldb/DOMAIN7-OTHERS.md

Then produce /home/work/hermesfixes/deerflow-modeldb/FINAL-SYNTHESIS.md containing:

## PART A: DATABASE SCHEMA

Design a complete SQL database schema (PostgreSQL-compatible DDL) that normalizes all provider intelligence data. Schema must include these tables with ALL columns, types, constraints, foreign keys, and indexes:

1. **providers** — id (UUID PK), slug (unique), name, website, description, category (enum: us_ai_lab, chinese_ai_lab, eu_ai_lab, open_source, api_aggregator, cloud_platform), region (enum: us, eu, cn, multi), auth_type (enum: api_key, oauth, token, none), status (enum: active, deprecated, merged), created_at, updated_at

2. **provider_plans** — id, provider_id (FK), slug, name, tier (enum: free, pro, team, enterprise, token_plan, edu, research, pay_as_you_go, subscription, custom), billing_model (enum: per_token, subscription, token_plan, hybrid), monthly_price_usd (nullable), description, rate_limit_rpm (nullable), rate_limit_rpd (nullable), rate_limit_tpm (nullable), rate_limit_concurrent (nullable), max_batch_size (nullable), supports_batch_api (bool), metadata (JSONB), created_at, updated_at

3. **models** — id, provider_id (FK), slug, display_name, model_family (e.g. GPT-4, Claude 4, Gemini 2.5), version_label, context_window_tokens (int), max_output_tokens (int), max_batch_output_tokens (nullable), training_cutoff_date (date or nullable), status (enum: active, beta, deprecated, sunset), released_date, deprecated_date, sunset_date (all nullable), description

4. **model_capabilities** — id, model_id (FK), capability (enum: text, vision, image_generation, audio, video, tool_use, function_calling, structured_output, streaming, code_execution, reasoning, extended_thinking, computer_use, grounding, search, file_upload, multilingual, json_mode), supported (bool), max_images_per_request (nullable), max_video_seconds (nullable), max_audio_seconds (nullable), notes (text)

5. **model_pricing** — id, model_id (FK), plan_id (FK, nullable — null means applies to all plans), endpoint_type (enum: standard, batch, streaming), region (varchar, e.g. us, eu), price_per_1m_input_tokens (decimal), price_per_1m_output_tokens (decimal), price_per_1m_cached_input_tokens (nullable), price_per_1m_cached_output_tokens (nullable), price_per_1m_input_tokens_batch (nullable), price_per_1m_output_tokens_batch (nullable), price_per_image (nullable), price_per_video_second (nullable), price_per_audio_second (nullable), effective_date (date), currency (varchar default 'USD'), metadata (JSONB)

6. **endpoints** — id, provider_id (FK), slug, name, base_url, version (e.g. v1), protocol (enum: https, websocket, grpc), auth_method (enum: bearer_token, api_key_header, api_key_query, oauth, custom), health_endpoint (nullable), docs_url (nullable), status (enum: active, deprecated, sunset), region (varchar)

7. **model_endpoint_map** — id, model_id (FK), endpoint_id (FK), plan_id (FK, nullable), is_default (bool), routing_weight (int default 100), notes

8. **model_aliases** — id, model_id (FK), alias (e.g. gpt-4o-2024-08-06), effective_start, effective_end (nullable), is_current (bool)

Include proper indexes, foreign key cascades, timestamps, and a full set of INSERT statements for EVERYTHING discovered in the domain files.

## PART B: SEED DATA

Write INSERT statements for every provider, plan, model, capability, pricing tier, endpoint, alias found across ALL 7 domain files. This must be complete enough that running the DDL + seed INSERTs produces a fully populated database.

Use provider/materialized IDs (not UUID) for readability: 'openai', 'anthropic', 'google', 'xai', 'deepseek', 'alibaba', 'zhipu', 'minimax', 'kimi', 'bytedance', 'mistral', 'openrouter', 'together', 'fireworks', 'groq', 'cohere', 'ai21', 'perplexity', 'meta', 'azure', 'aws', 'huggingface', 'reka', 'yi', 'baichuan'

## PART C: DATA FRESHNESS

Note which data points came from actual pricing pages vs inferred. Flag any items that need periodic refresh and suggest a refresh cadence per provider.

## PART D: JSON EXPORT

Also produce a JSON export format that mirrors the SQL schema — one JSON file per table as arrays of objects.

## OUTPUT LOCATION
Write everything to /home/work/hermesfixes/deerflow-modeldb/FINAL-SYNTHESIS.md

DO NOT stop until ALL domain files exist and the synthesis file is COMPLETE.
